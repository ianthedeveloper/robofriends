import React from "react";

const SearchBox = ({searchfield}) => {
    return (
        <div>
            <input className="pa3 ba b--blue bg-light-blue" type="search" placeholder="search robot"/>
        </div>
    );
}

export default SearchBox;